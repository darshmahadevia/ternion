# Domain Docs

QuorumKV is a single-context repository.

Before working, read `CONTEXT.md` and every relevant ADR under `docs/adr/`. Use the glossary's canonical vocabulary and avoid its rejected synonyms. If proposed work conflicts with an ADR, surface the conflict rather than silently overriding it.
